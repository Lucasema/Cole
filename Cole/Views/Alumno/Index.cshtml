@model IEnumerable<Cole.Models.Alumno>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Persona.Contraseña)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tutor.Ocupacion)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.Contraseña)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tutor.Ocupacion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Dni }) |
                @Html.ActionLink("Details", "Details", new { id=item.Dni }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Dni })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
